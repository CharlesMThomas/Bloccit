<%= f.label :rating %>
<% if rating %>
  <%= f.select :rating, options_for_select(Rating.severities, Rating.severities[rating]) %>
<% else %>
  <%= f.select :rating, options_for_select(Rating.severities) %>
<% end %>
